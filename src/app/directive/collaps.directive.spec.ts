import { CollapsDirective } from './collaps.directive';

describe('CollapsDirective', () => {
  it('should create an instance', () => {
    const directive = new CollapsDirective();
    expect(directive).toBeTruthy();
  });
});
